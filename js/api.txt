// build: esbuild bin.js --bundle --target=es2020 --format=esm --outfile=bin.bundle.js --watch

count
	count(first, last, symbol, { groupLsb })
	countBatch(first, last, sortedSymbols, { groupLsb })
	countLessThan(first, last, upper)
	countRange(first, last, lower, upper)
	counts(first, last, lower, upper, { groupLsb, sorted,  })

quantile
	quantile(first, last, index)
	quantileBatch(first, last, sortedIndices)
	quantiles(first, last, firstIndex, lastIndex)

symbol
	symbol(index)
	symbolBatch(sortedIndices) // todo
	symbols(first, last)

find
	find(first, last, symbol, count)

majority
	majority(first, last, k) // todo: k

===

revision proposal

	countRange => count
	count => countSymbol
	countBatch => countSymbols

	// not sure how to preserve symmetry with quantile/symbol, but i confused countRange with count today.