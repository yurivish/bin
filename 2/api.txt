count
	count(first, last, symbol, symbolBlockBits)
	countBatch(first, last, sortedSymbols, symbolBlockBits)
	countLessThan(first, last, upper)
	countRange(first, last, lower, upper)
	counts(first, last, lower, upper, symbolBlockBits)

quantile
	quantile(first, last, index)
	quantileBatch(first, last, sortedIndices)
	quantiles(first, last, firstSortedIndex, lastSortedIndex)

symbol
	symbol(index)
	symbolBatch(sortedIndices)
	symbols(first, last)

majority
	majority(first, last, k)
