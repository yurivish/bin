count
	countSymbol(first, last, symbol)
	countSymbols(first, last, symbols) // todo
	countLess(first, last, upper)
	counts(first, last, lower, upper, symbolBlockBits) // = report

quantile
	quantile(first, last, sortedIndex)
	// symbols and their frequencies in ascending order by symbol
	// from New algorithms on wavelet trees and applications to information retrieval
	// called mrqq in the paper
	quantiles(first, last, firstSortedIndex, lastSortedIndex) // todo
	quantiles(first, last, sortedIndices) // useful for majority

access
	access(i)
	accessIndices(indices)
	accessAll(first, last)

majority
	majority(first, last, k)
