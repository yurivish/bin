count
	countSymbol(first, last, symbol, symbolBlockBits)
	countSymbols(first, last, symbols, symbolBlockBits)
	countLessThan(first, last, upper)
	countRange(first, last, lower, upper)
	allCounts(first, last, lower, upper, symbolBlockBits) // = report

quantile
	quantile(first, last, sortedIndex)
	quantiles(first, last, sortedIndices) // useful for majority
	allQuantiles(first, last, firstSortedIndex, lastSortedIndex)

access
	accessSymbol(i)
	accessSymbols(indices)
	accessAllSymbols(first, last)

majority
	majority(first, last, k)
